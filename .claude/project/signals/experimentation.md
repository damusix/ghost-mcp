# experimentation

## What it does

Provides a disposable local Ghost 6 + MySQL 8 stack (via Docker Compose) and noorm tooling to inspect Ghost's live database schema and extract API keys, so the MCP server can be exercised against a real Admin/Content API without a production instance. Includes a Node.js harness ([`scripts/koenig/`](../../../scripts/koenig)) that round-trips all 23 Koenig editor card types through the local Admin API to derive and validate the canonical lexical payload reference.

## Artifacts

- [`docker-compose.yml`](../../../docker-compose.yml) — brings up `ghost:6-alpine` on port 2368 and `mysql:8.0` on port 3306; both services use named volumes (`ghost-mysql`, `ghost-content`) that persist across restarts; host ports overridable via `GHOST_PORT` and `MYSQL_PORT` env vars
- [`bin/ghost-keys.sh`](../../../bin/ghost-keys.sh) — derives `GHOST_ADMIN_API_KEY` and `GHOST_CONTENT_API_KEY` from the live MySQL DB via noorm SQL queries against `api_keys` and `integrations` tables; prints keys or writes [`.env`](../../../.env) with `--write`
- [`.noorm/settings.yml`](../../../.noorm/settings.yml) — noorm project config: [`sql`](../../../sql) → [`./sql`](../../../sql), [`changes`](../../../changes) → [`./changes`](../../../changes); defines a `dev` stage (`mysql`, `127.0.0.1:3306`, database `ghost`, `protected: true`) with logging silenced by default
- [`.noorm/dev.env`](../../../.noorm/dev.env) — env-only connection vars (`NOORM_CONNECTION_*`) for noorm against the Docker MySQL; matches credentials and port in [`docker-compose.yml`](../../../docker-compose.yml)
- [`sql/`](../../../sql) — placeholder directory for noorm-managed SQL files (currently empty)
- [`changes/`](../../../changes) — placeholder directory for noorm-managed schema change files (currently empty)
- [`.mcp.json`](../../../.mcp.json) — registers the noorm MCP server (`noorm mcp serve`) for DB inspection via MCP tools in Claude sessions

## Scripts

- [`scripts/koenig/cards.mjs`](../../../scripts/koenig/cards.mjs) — source-of-truth candidate lexical payloads for all 23 Koenig card types (paragraph, heading, image, gallery, video, audio, file, bookmark, embed, html, markdown, codeblock, callout, toggle, button, header, call-to-action, signup, product, horizontalrule, paywall, email, email-cta); uses `static.ghost.org` demo media assets
- [`scripts/koenig/roundtrip.mjs`](../../../scripts/koenig/roundtrip.mjs) — POSTs each card to the local Admin API as a draft post, reads back stored lexical + rendered HTML, reports accept/normalize/render status per card; deletes probes after
- [`scripts/koenig/capture.mjs`](../../../scripts/koenig/capture.mjs) — POSTs each card, reads back Ghost's normalized stored lexical, writes [`docs/koenig-cards.json`](../../../docs/koenig-cards.json)
- [`scripts/koenig/gen-doc.mjs`](../../../scripts/koenig/gen-doc.mjs) — regenerates [`docs/koenig-cards.md`](../../../docs/koenig-cards.md) from [`docs/koenig-cards.json`](../../../docs/koenig-cards.json) plus per-card group/render-behavior/field notes defined inline in the script
- [`scripts/koenig/gallery.mjs`](../../../scripts/koenig/gallery.mjs) — publishes one "Koenig Card Gallery" post to the local Ghost instance containing every card for visual inspection in the editor/frontend
- [`scripts/koenig/README.md`](../../../scripts/koenig/README.md) — documents the harness, prereqs (stack up + [`.env`](../../../.env)), and the regenerate sequence

## Docs

- [`docs/experimentation.md`](../../../docs/experimentation.md) — setup guide: stack start/stop commands, first-time Ghost activation curl, Admin UI URL, key extraction via [`bin/ghost-keys.sh`](../../../bin/ghost-keys.sh), noorm env-only usage patterns, and caveats (noorm bookkeeping tables, `protected` flag scope)
- [`docs/koenig-cards.md`](../../../docs/koenig-cards.md) — canonical human-readable reference for all 23 Ghost 6 Koenig card lexical payloads; generated by [`scripts/koenig/gen-doc.mjs`](../../../scripts/koenig/gen-doc.mjs) from the round-tripped JSON; includes document envelope shape, per-card field notes, and the `format` bitmask table
- [`docs/koenig-cards.json`](../../../docs/koenig-cards.json) — machine-readable canonical form: Ghost's normalized stored lexical for each card type, written by [`scripts/koenig/capture.mjs`](../../../scripts/koenig/capture.mjs); 322 lines / 23 top-level keys

## Coupling

- [`bin/ghost-keys.sh`](../../../bin/ghost-keys.sh) sources [`.noorm/dev.env`](../../../.noorm/dev.env) at runtime — if the env file moves or credentials change, the script breaks
- `bin/ghost-keys.sh --write` overwrites [`.env`](../../../.env) at repo root — this is the same [`.env`](../../../.env) consumed by [`src/ghost-client.ts`](../../../src/ghost-client.ts) for `GHOST_ADMIN_API_KEY` and `GHOST_CONTENT_API_KEY`; mcp-server domain is the consumer
- All five `scripts/koenig/*.mjs` scripts read [`.env`](../../../.env) at repo root for `GHOST_URL` and `GHOST_ADMIN_API_KEY`; same source as the [`.env`](../../../.env) produced by `bin/ghost-keys.sh --write`
- [`scripts/koenig/capture.mjs`](../../../scripts/koenig/capture.mjs) writes [`docs/koenig-cards.json`](../../../docs/koenig-cards.json); [`scripts/koenig/gen-doc.mjs`](../../../scripts/koenig/gen-doc.mjs) reads it — run capture before gen-doc
- [`docker-compose.yml`](../../../docker-compose.yml) credentials must stay in sync with [`.noorm/dev.env`](../../../.noorm/dev.env); they are currently identical throwaway values (`root`/`ghost`)
- noorm requires [`sql/`](../../../sql) and [`changes/`](../../../changes) to exist (configured in [`.noorm/settings.yml`](../../../.noorm/settings.yml)); removing them would break `noorm` commands

## Conventions worth knowing

- The stack uses throwaway credentials (`root`/`ghost`) — not reused in any real environment
- noorm is used in env-only mode (no `noorm init`, no TTY); load vars with `set -a; source .noorm/dev.env; set +a` before running noorm commands
- `protected: true` on the `dev` stage only guards against `noorm db teardown` when using a stored config; env-only mode does not carry the flag
- `--json sql` output from noorm emits one JSON object per row plus header/footer lines; filter with `grep '^{'` before piping to `jq`
- [`.noorm/state/`](../../../.noorm/state) is gitignored; `docker compose down -v` wipes all Docker volumes and resets the Ghost schema
- All koenig scripts authenticate via a self-signed JWT (HS256, 5-minute exp, audience `/admin/`) computed inline — same algorithm as [`src/ghost-client.ts`](../../../src/ghost-client.ts), no shared library
- Regenerate sequence: `node scripts/koenig/capture.mjs` → `node scripts/koenig/gen-doc.mjs` → optionally `node scripts/koenig/gallery.mjs`
- `cards.mjs` exports a named `cards` object keyed by card type; all other scripts import from it
